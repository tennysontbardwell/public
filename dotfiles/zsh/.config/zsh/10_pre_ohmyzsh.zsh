POWERLEVEL9K_MODE='awesome-fontconfig' # for fonts to show up correctly
# if [ "$TERM" != "dumb" ]; then
#     export TERM="xterm-256color"
# fi

autoload bashcompinit
bashcompinit

autoload -Uz compinit
compinit
